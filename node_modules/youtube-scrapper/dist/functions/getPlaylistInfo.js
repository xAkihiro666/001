"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPlaylistInfo = void 0;
const Playlist_1 = require("../structures/Playlist");
const Util_1 = require("../util/Util");
async function getPlaylistInfo(urlOrId, options = {}) {
    const listId = Util_1.Util.getListId(urlOrId);
    const playlist = new Playlist_1.Playlist(listId);
    if (options.full) {
        await playlist.fetch();
    }
    else {
        await playlist.fetchFirstPage();
    }
    return playlist;
}
exports.getPlaylistInfo = getPlaylistInfo;

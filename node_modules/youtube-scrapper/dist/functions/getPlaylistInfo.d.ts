import { Playlist } from "../structures/Playlist";
export interface GetPlaylistInfoOptions {
    full?: boolean;
}
export declare function getPlaylistInfo(urlOrId: string, options?: GetPlaylistInfoOptions): Promise<Playlist>;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.downloadFromVideo = void 0;
/**
 * Downloads a
 * @param video
 * @param format
 * @returns
 */
function downloadFromVideo(video, format, options) {
    if (!format)
        format = video.formats.find(c => c.quality !== "LOW");
    if (!format) {
        throw new Error(`Could not find suitable format for this download.`);
    }
    return video.download(format, options);
}
exports.downloadFromVideo = downloadFromVideo;
